export type GameResultResponse = {
  newScore: number;
  play: number;
  win: boolean;
};

export type PlayResult = {
  newScore: number;
  play: number;
};

export type AvailableResponse = {
  available: boolean;
};
